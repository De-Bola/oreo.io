	unsigned char arrayA[FIFO_BUFFER_SIZE] = "Adebola Gbiri is a good man";
	unsigned int output = 0;
	output = fifo_putc(&list, 'A');
	
	//unsigned int show;

	//for(i = 0; i < 31; i++){
	//    output = fifo_putc(&list, arrayA[i]);
	//}

	//show = output;

	//output;// = fifo_getc(&list);